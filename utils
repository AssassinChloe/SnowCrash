#VM's Settings->Network on pass Attached to a Host-only Adapter

#for navigating into the VM with local console
#recommanded ssh connection at port 4242: 
    ssh levelXX@IPmachine -p 4242

#for copying files from the VM to local
    scp -P 4242 levelXX@IPmachine:/absolute/path/of/file wheretocopy

#for analyzing code and find addresses
    gdb binary
    disas main
    break
    jump
    run
    continue
    set
    s/x

#tools
    dcode.fr
    wireshark
    john the ripper
    dog bolt
    gdb
    chatGPT